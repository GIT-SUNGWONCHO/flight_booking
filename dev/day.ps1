# 다음 09:00 에 무엇을 잴 것인가 — 이 파일 한 줄만 고친다.
#
# 전날 사전점검(precheck.ps1)과 아침 측정(morning.ps1)이 이 파일을 같이 읽는다.
# 두 곳에 따로 적으면 점검한 화면과 측정하는 화면이 어긋난다. 그래서 한 곳이다.
#
# 비워두면($DailyArgs = @()) 요일로 자동 판단한다
#   — 열리는 날이 월·수·토면 로마, 아니면 파리.
#
# 2026-09-05 (토, 열리는 날 2027-08-31 화 = 로마 운항일 아님):
#   파리→인천(CDG→ICN). 유럽발 경로를 한 번 더 밟는다 - 9/9 실전이 유럽발인데
#   그 경로는 09:00 실조건을 아직 한 번도 완주한 적이 없다. (9/25 목표와도 같은 방향)
#   09-06 은 로마가 뜨므로 그날 FCO 로 바꾼다 - 실전 전 마지막 로마 연습이다.
$DailyArgs = @('--route', 'ICN', '--from', 'CDG')
