# 백로그

지금 도구는 **가는 편 · 단일 계정 · 마일리지 예매** 하나를 09:00 에 밟는 것까지 한다.
아래는 그 다음 단계들. 위에서부터 우선순위.

---

## 1. 캘린더 단계 건너뛰기 — 항공편 화면에서 바로 시작

**지금**: 달력에서 날짜 → 검색 → 항공편 화면. 09:00 에 이 3단계를 밟느라 시간을 쓴다.

**하고 싶은 것**: `/booking/select-award-flight/departure` 를 미리 띄워두고 거기서
날짜만 바꿔 바로 조회. 그 화면 상단에 날짜 스트립(16·17·18…)이 있으므로,
달력을 거치지 않고 스트립에서 목표 날짜를 눌러 시작할 수 있는지.

**확인할 것**
- 그 URL 을 직접 열었을 때 세션/검색조건이 유지되는가 (아니면 검색 페이지로 튕기는가)
- 날짜 스트립에 아직 안 열린 날짜가 나타나는 타이밍이 달력과 같은가
- 통화(KRW)가 이 경로에서도 되돌아가는가
- 줄어드는 시간이 실제로 얼마인가 — 지금 로그로 1~3단계가 몇 초인지 재보면 된다

**주의**: 통화를 바꾸면 화면이 처음으로 되돌아간다(restartFrom). 시작점을 바꾸면
그 되돌아가는 지점도 같이 바뀌므로 재확인 필요.

---

## 2. 여러 계정 · 여러 노선 동시 시도

**하고 싶은 것**
- 두 계정(본인 + 배우자)으로 각각 시도
- 같은 계정으로 여러 노선 동시 시도

**구조상 문제**
- 계정은 쿠키 단위다. 같은 브라우저 프로필에서 두 계정 로그인은 불가능하다.
  → **크롬 프로필을 분리**하거나 시크릿 창을 쓰는 수밖에 없다.
- 재생 상태(`ke_award_steps_v1`)는 localStorage 라 **오리진 단위로 공유**된다.
  같은 프로필의 새 탭 두 개가 서로의 `idx` 를 덮어쓴다. 지금 구조로는 동시 실행이
  깨진다. 탭마다 다른 키를 쓰게 하거나(예: `sessionStorage` 로 이전), 아예
  프로필을 나눠야 한다.
- 한 계정 여러 노선도 같은 문제. 노선별로 상태가 분리돼야 한다.

**먼저 정할 것**: 프로필 분리로 갈지(간단·확실, 창이 여러 개), 한 프로필에서
탭별 상태 분리로 갈지(복잡, 계정은 여전히 하나). 계정이 둘이면 프로필 분리는 필수다.

**주의**: 같은 계정으로 동시에 여러 예매를 시도하면 사이트가 세션 충돌로 막을 수
있다. 소규모로 먼저 확인할 것.

---

## 3. 보너스 대기 예약 매크로 (별도)

**URL**: `/booking/select-award-wait-flight/departure`

마일리지 예매가 실패했을 때의 대안. **지금 매크로와 완전히 다른 흐름**이므로
별도 녹화 · 별도 steps 로 가야 한다.

**필요한 것**
- steps 를 여러 벌 저장하고 고르는 기능 (지금은 한 벌뿐)
- 대기 예약 흐름 녹화 (Alt+P 로 셀렉터 확보)
- 마일리지 예매와 **동시 실행** 가능한지 — 2번의 상태 분리 문제와 같은 뿌리다

**순서**: 2번(상태 분리)을 먼저 해결해야 3번 동시 실행이 가능하다. 다만 대기 예약
매크로 자체는 먼저 만들어두고 단독으로 돌려볼 수 있다.
